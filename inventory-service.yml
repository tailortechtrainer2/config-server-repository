server:
  port: 8083

spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:mysql://localhost:3306/ECOM
    username: root
    password: FrogJump@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: MySecretTokenForJwtTokenGeneration1234567890

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
